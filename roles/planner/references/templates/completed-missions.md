<!--
TEMPLATE — structure only. Copy to `completed-missions.md` at the fleet repo root, fill in, commit + push.
The doctrine (complete vs done, the winding-down phases, why the conv id is the asset) lives in the `mission-boards` skill.
Replace <…> placeholders. Delete the example row once you have real ones. Keep it fleet-generic: no host-specific tooling.
-->

# Completed Missions — <fleet> (post-completion record)

| Mission | Project | PR · state | Handler conv id (live · pane-confirmed) | Handler worktree | Branch | Phase |
|---|---|---|---|---|---|---|
| <name> | <project> | <#n · merged date / #n · open, worktree kept / none (review, docs, investigation)> | `<uuid>` | `…--<worktree>` | `<branch>` | <cleanup / post-mortem> |

## Notes

- <anything that lives nowhere else: PR states to re-confirm, conv-id re-seats, missions with no dedicated worktree>
