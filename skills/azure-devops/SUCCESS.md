# azure-devops — SUCCESS

The criteria for judging whether this skill was followed. The SKILL.md is based on this file, not the other way around.

## Not marked

azure-devops is the foundation for the ADO skills: org/project detection from the git remote, the `ado-rest.sh` REST wrapper, sub-skill routing, and auth-token troubleshooting. It is all means, not a standard — if detection or the wrapper is wrong, the sub-skill call fails visibly, where the real work is judged.

So there is nothing to mark here. The ADO output that matters is judged in the skills that produce it (azure-devops-pr, ado-work-items); this foundation is not a standard to penalise against.
