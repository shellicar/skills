# prompt-authoring: editorial context

## Provenance

Moved from `fleet/references/prompt-authoring.md` (in `claude-fleet-shellicar/fleet/`) on 2026-06-26, as part of the actor/role/skill refactor — the strangler-fig migration recorded in `~/.claude/taxonomy-refactor.md`.

That reference was one mixed file. It split three ways:

- the reusable prompt-craft → this skill (`SKILL.md`);
- the scribe's disposition (record what the SC decided, never invent) → `roles/scribe/`;
- fleet-specific mechanics (scaffold script, blocks, submodule refresh, skills-selection) → stayed in the fleet.

The `SKILL.md` content is byte-faithful from the source — only the title changed. The straddler sections (Writing-for-a-literal-reader, Findings-not-rules) and stale terminology ("Project Manager (you)") are carried as-is; re-sorting and cleanup are a later authoring pass, not this move.
