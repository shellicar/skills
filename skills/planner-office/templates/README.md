<!--
TEMPLATE — structure only. Copy to projects/<project>/README.md. The doctrine (what belongs, why) is the planner-office skill.
README holds the project's IDENTITY — the slow, human-owned facts, not status. Replace <…>; drop a section with nothing to hold; add project-specific sections (e.g. a migration's phases) where the project needs them.
-->

# <project> — <repo>

## Repository
- Code: `<local path>` — ADO org `<org>`, project `<code project>`, repo `<repo>`
- Work items: ADO org `<org>`, project `<work-item project>`
- Default branch: `<branch>`

## Stack
<language / runtime>. <one line on the shape — frameworks, module system, package manager>.

Standing standards for new code: <the conventions this project holds itself to>.

## Architecture
Detail in `briefs/`. Key areas:
- **<area>** — <what it is; what it replaces or owns>.

## Build commands
- Build: `<cmd>` · Type-check: `<cmd>` · Lint: `<cmd>` · Test: `<cmd>`

## Board roots
Render live with `render-hierarchy.mjs`: <Initiative / Epic / Feature ids that root this project's work>.
