# git-workflow — SUCCESS

Not marked. Everything with teeth is judged in a marked skill — commit format under sc-commit-writing, staging and bypasses under safe-operations, the staged set under pre-commit.
