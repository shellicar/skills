# git-workflow — SUCCESS

The criteria for judging whether this skill was followed. The SKILL.md is based on this file, not the other way around.

## Not marked

git-workflow is reference — how commits, staging, and pushes should look. Everything in it that carries verification teeth already lives in a marked skill: commit-message format under sc-commit-writing, explicit staging and the `--no-verify` bypass under safe-operations, the staged-set check under pre-commit. What is purely its own — `git push -u origin <branch>` for a new branch — carries no value worth a PASS/FAIL.

So there is nothing to mark here. The git output that matters is judged where it has teeth; this reference is not a standard to penalise against.
