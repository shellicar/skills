# sc-ghostwriting — SUCCESS

The criteria for judging whether this skill was followed. The SKILL.md is based on this file, not the other way around.

## What this verifies

Whether output authored with the Supreme Commander's name attached sounds like him. It is the voice layer beneath every `sc-` output — commit messages, PRs, work items — and anything else that goes out in his name (messages, docs). Authorship triggers it, not genre.

## Where to look

Everywhere the session put his name on something: the commit message (sc-commit-writing's output), the PR (sc-pr-writing's), the work item (sc-workitem-writing's), and any message or doc authored as him. The voice is verified *on those artifacts* — so checking this skill means looking across the other `sc-` skills' outputs and the surrounding context, not at one place of its own.

## How to judge

### Em dashes — a single one is a FAIL

The one binary rule. An em dash (`—`) anywhere in output under his name is a fail — not graded, not a forgivable slip. One is enough.

### Voice — judgment

Direct, concrete, not hedged, not formal. This is voice-matching, so it is a judgment, not a checklist: hedged or formal output is a red flag that raises scrutiny, rarely a clean fail on its own, and often inconclusive from a single short artifact. (The skill is thin today — em dashes and directness; it grows as more of his voice is captured.)

### N/A

The session authored nothing under his name. Authorship triggers the skill; with no named output, it had no occasion — known not to apply.

### INCONCLUSIVE

You can't see the named outputs — truncated pane, the artifacts not visible — so you can't check them. "I can't verify."
